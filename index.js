const selectBtnEl = document.getElementById("selectBtn");

const colorSelectorEl = document.getElementById("colorSelector");


selectBtnEl.addEventListener('submit', function (event) {
    
})

/*
fetch("https://www.thecolorapi.com/id{?hex,rgb,hsl,cmyk,format}", {
    method: "GET",
    headers: {"Content-Type":"application/json"}
})
    .then(response => response.json())
    .then(data => {
        console.log(data);
    })
*/